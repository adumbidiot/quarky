[package]
name = "quarky"
version = "0.0.2"
authors = [ "adumbidiot <nathaniel.daniel23@outlook.com>" ]
edition = "2024"
publish = false
license = "MIT"

[dependencies]
anyhow = "1.0.104"
argh = "0.1.19"
audiopus_sys = { version = "0.2.2", default-features = false }
camino = { version = "1.2.5", features = [ "serde1" ] }
# clokwerk = "0.4.0"
clokwerk = { git = "https://github.com/mdsherry/clokwerk" }
chrono = { version = "0.4.45", default-features = false }
fern = { version = "0.7.1", features = [ "colored" ] }
http = "1.5.0"
indexmap = "2.14.1"
lazy_static = "1.5.0"
log = "0.4.34"
nitter = { path = "lib/nitter", features = [ "rustls" ], default-features = false }
poise = "0.6.2"
rand = "0.10.2"
reddit = { git = "https://github.com/adumbidiot/reddit-rs", default-features = false, features = [ "rustls-tls" ] }
# This should match the version used by songbird.
# Serenity usually ships an older version,
# usually in conflict with songbird.
reqwest = { version = "=0.12.28", default-features = false, features = [ "rustls-tls" ] }
rss-client = { path = "lib/rss-client", default-features = false, features = [ "rustls" ] }
serde = { version = "1.0.229", features = [ "derive" ] }
serde_json = "1.0.151"
serenity = "0.12.5"
songbird = "0.6.0"
thiserror = "2.0.20"
tokio = { version = "1.53.1", features = [ "rt-multi-thread", "signal" ] }
toml = "1.1.4"
zalgo = { git = "https://github.com/adumbidiot/zalgo-rs" }

[features]
default = []
pkg-config = [
    "audiopus_sys/pkg-config",
]

[profile.release]
lto = "fat"
codegen-units = 1
debug = false
opt-level = 3
debug-assertions = false
strip = "symbols"

[patch.crates-io]
audiopus_sys = { git = "https://github.com/adumbidiot/audiopus_sys", branch = "pkg-config" }

[workspace]
members = [
    "lib/*"
]

[package.metadata.deb]
name = "quarky"
depends = [ "libopus0", "yt-dlp" ]
assets = [
    [ "target/release/quarky", "usr/bin/", "700" ],
    [ "deb-config.toml", "etc/quarky/config.toml", "600" ],
]
conf-files = [
    "etc/quarky/config.toml",
]
maintainer-scripts = "debian/"

[package.metadata.deb.systemd-units]
enable = false
start = false
