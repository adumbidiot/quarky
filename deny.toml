targets = [
	{ triple = "x86_64-unknown-linux-gnu" },
	{ triple = "x86_64-unknown-linux-musl" },
	{ triple = "x86_64-apple-darwin" },
	{ triple = "x86_64-pc-windows-msvc" },
]

[advisories]
vulnerability = "deny"
unmaintained = "deny"
notice = "deny"
ignore = [
	# blocked on tokio bumping mio to 0.7
	"RUSTSEC-2020-0016",
    
    # blocked on xsalsa20poly1305 being bumped to 0.6.0
    "RUSTSEC-2020-0057",
    "RUSTSEC-2020-0058",
]

[licenses]
unlicensed = "deny"
copyleft = "warn"
confidence-threshold = 0.93
default = "deny"
private = { ignore = true }
allow = [ "Apache-2.0", "MIT", "ISC", "Zlib", "BSD-3-Clause" ]
exceptions = [
    { name = "ring", allow = [ "MIT", "ISC", "OpenSSL" ] },
    { name = "webpki-roots", allow = [ "MPL-2.0" ] },
    { name = "egg-mode", allow = [ "MPL-2.0" ] },
    { name = "colored", allow = [ "MPL-2.0" ] },
]

[[licenses.clarify]]
name = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [
    { path = "LICENSE", hash = 0xbd0eed23 },
]

[[licenses.clarify]]
name = "webpki"
expression = "ISC"
license-files = [
    { path = "LICENSE", hash = 0x001c7e6c },
]

[bans]
multiple-versions = "deny"
highlight = "all"
skip = [
	# blocked on tokio bumping mio to 0.7
	{ name = "winapi", version = "0.2.8" },
	
    # blocked on serenity bumping async-tungstenite to 0.5.0
    { name = "webpki-roots", version = "0.19.0" },
	
    # blocked on mio-named-pipes depending on a single version of miow/mio
    { name = "miow", version = "0.3.4" },
    
    # blocked on futures-util, hyper, and tracing-futures bumping pin-project-internal and pin-project to 1.0.x
    { name = "pin-project-internal", version = "0.4.27" },
    { name = "pin-project", version = "0.4.27" },
    
    # blocked on egg-mode, reqwest, rustls, and tungstenite bumping base64 to 0.13.x
    { name = "base64", version = "0.12.3" },
    
    # blocked on getrandom, log, mio, net2, openssl, socket2, tempfile, and tracing bumping cfg to 1.0.x
    { name = "cfg-if", version = "1.0.0" },
]

[sources]
unknown-registry = "warn"
unknown-git = "deny"
allow-git = [
	"https://github.com/adumbidiot/zalgo-rs",
	"https://github.com/adumbidiot/reddit-rs",
]